desc 'jekyll serve'
task :default do
   sh "bundle exec jekyll serve"
 end

desc 'bundle install'
task :install do
   sh 'bundle install'
 end

desc 'generate drafts'
task :drafts do
  sh 'bundle exec jekyll serve --drafts'
end
